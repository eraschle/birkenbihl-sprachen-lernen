"""Birkenbihl GUI - Graphical user interface (future implementation).

This module will contain the GUI implementation using a framework like
PyQt, Tkinter, or a web-based interface. It will use the same app.py
services as the CLI, ensuring consistent business logic.

Example GUI frameworks to consider:
    - PyQt6/PySide6: Native desktop GUI
    - Tkinter: Built-in Python GUI (simpler)
    - Streamlit: Web-based UI (rapid prototyping)
    - FastAPI + HTML: Custom web interface
"""


def start_gui() -> None:
    """Start the GUI application.

    This function will initialize and launch the GUI when implemented.

    Planned features:
        - Translation input form
        - Results display with formatting
        - Translation history browser
        - Settings panel for provider/model selection
    """
    raise NotImplementedError("GUI not yet implemented")
