# ============================================================================
# Birkenbihl Environment Variables (OPTIONAL)
# ============================================================================
#
# This file is OPTIONAL if you store API keys directly in settings.yaml.
# Use this approach only if you prefer environment variables over YAML config.
#
# ============================================================================

# ----------------------------------------------------------------------------
# AI Provider API Keys (Optional)
# ----------------------------------------------------------------------------
# Alternative to storing API keys in settings.yaml.
# If you use this approach, reference them in settings.yaml like:
#   api_key: ${OPENAI_API_KEY}
#
# NOTE: For most users, storing keys directly in settings.yaml is simpler.

# OpenAI API Key
# OPENAI_API_KEY=your_openai_api_key_here

# Anthropic API Key
# ANTHROPIC_API_KEY=your_anthropic_api_key_here

# ----------------------------------------------------------------------------
# Storage Configuration (Optional)
# ----------------------------------------------------------------------------
# Override default storage location (~/.birkenbihl/translations.json)
# BIRKENBIHL_STORAGE_PATH=/path/to/custom/storage.json

# Database URL for SQLite storage (future feature)
# DATABASE_URL=sqlite:///translations.db

# ============================================================================
# Setup Instructions
# ============================================================================
#
# Option 1 (Recommended): API Keys in settings.yaml
#   - Store keys directly in settings.yaml (easier, no env vars needed)
#   - See settings.yaml.example for details
#
# Option 2: API Keys in .env (this file)
#   - Copy this file to .env: cp .env.example .env
#   - Uncomment and add your API keys above
#   - Reference them in settings.yaml: api_key: ${OPENAI_API_KEY}
#
# ============================================================================
