# ============================================================================
# Birkenbihl Multi-Provider Configuration
# ============================================================================
#
# This file supports two configuration formats:
#
# 1. NEW FORMAT (recommended): Multiple providers with individual configurations
# 2. LEGACY FORMAT: Single provider with API keys (automatically converted)
#
# ============================================================================

# ----------------------------------------------------------------------------
# NEW FORMAT: Multi-Provider Configuration
# ----------------------------------------------------------------------------
# Configure multiple AI providers with different models and API keys.
# Each provider is identified by an index (0, 1, 2, etc.)

# Provider 0: OpenAI GPT-4
BIRKENBIHL_PROVIDER_0_NAME=OpenAI GPT-4
BIRKENBIHL_PROVIDER_0_TYPE=openai
BIRKENBIHL_PROVIDER_0_MODEL=gpt-4
BIRKENBIHL_PROVIDER_0_API_KEY=your_openai_api_key_here
BIRKENBIHL_PROVIDER_0_DEFAULT=true

# Provider 1: Claude Sonnet (example additional provider)
# BIRKENBIHL_PROVIDER_1_NAME=Claude Sonnet
# BIRKENBIHL_PROVIDER_1_TYPE=anthropic
# BIRKENBIHL_PROVIDER_1_MODEL=claude-3-5-sonnet-20241022
# BIRKENBIHL_PROVIDER_1_API_KEY=your_anthropic_api_key_here
# BIRKENBIHL_PROVIDER_1_DEFAULT=false

# General Settings
BIRKENBIHL_TARGET_LANGUAGE=de

# ----------------------------------------------------------------------------
# LEGACY FORMAT (backward compatible)
# ----------------------------------------------------------------------------
# If you use this format, it will be automatically converted to the new format.
# The BIRKENBIHL_DEFAULT_MODEL determines which provider is default.

# OPENAI_API_KEY=your_openai_api_key_here
# ANTHROPIC_API_KEY=your_anthropic_api_key_here
# BIRKENBIHL_DEFAULT_MODEL=openai:gpt-4
# BIRKENBIHL_TARGET_LANGUAGE=de

# ----------------------------------------------------------------------------
# Storage Configuration (optional)
# ----------------------------------------------------------------------------
# BIRKENBIHL_STORAGE_PATH=/path/to/custom/storage.json
# DATABASE_URL=sqlite:///translations.db
